apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  labels:
    app: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-container
          image: codingsprints/mern-space-notification-service-prod:build-3
          ports:
            - containerPort: 5005
          volumeMounts:
            - name: notification-secret
              mountPath: "/home/node/app/config"
              readOnly: true
      volumes:
        - name: notification-secret
          secret:
            secretName: notification-secret

---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 5005
      targetPort: 5005
